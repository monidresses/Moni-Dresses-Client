import{auth}from'./firebase.js';import{onAuthStateChanged,signOut}from'https://www.gstatic.com/firebasejs/12.17.1/firebase-auth.js';const userEl=document.getElementById('user'),logout=document.getElementById('logout');let redirecting=false;onAuthStateChanged(auth,u=>{if(redirecting)return;if(!u){redirecting=true;location.replace(`/login.html?redirect=${encodeURIComponent('account.html')}`);return}userEl.textContent=u.email||u.phoneNumber||'Signed in';logout.disabled=false});logout.disabled=true;logout.addEventListener('click',async()=>{logout.disabled=true;try{await signOut(auth);location.replace('/')}catch(e){console.error(e);logout.disabled=false;userEl.textContent='Unable to sign out. Please try again.'}});