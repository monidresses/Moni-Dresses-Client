import{db}from'./firebase.js';import{doc,getDoc}from'https://www.gstatic.com/firebasejs/12.17.1/firebase-firestore.js';import{addToCart}from'./cart.js';const id=new URLSearchParams(location.search).get('id');const root=document.getElementById('product');async function load(){if(!id){root.textContent='Product not found.';return}const snap=await getDoc(doc(db,'products',id));if(!snap.exists()||snap.data().active===false){root.textContent='Product unavailable.';return}const p=snap.data();root.innerHTML=`<article><h1>${escapeHtml(p.name||'Product')}</h1><p>₹${Number(p.price||0)}</p><p>${escapeHtml(p.description||'')}</p><button id="add">Add to Cart</button></article>`;document.getElementById('add').onclick=()=>{addToCart({id,name:p.name,price:p.price,image:p.image});location.href='/cart.html'}}function escapeHtml(v){const d=document.createElement('div');d.textContent=v;return d.innerHTML}load().catch(e=>{console.error(e);root.textContent='Unable to load product.'});