import{db}from'./firebase.js';import{collection,getDocs,query,where,limit}from'https://www.gstatic.com/firebasejs/12.17.1/firebase-firestore.js';import{shop}from'./app-config.js';const root=document.getElementById('products');async function load(){try{const snap=await getDocs(query(collection(db,'products'),where('active','==',true),limit(100)));root.replaceChildren(...snap.docs.map(d=>{const p=d.data(),a=document.createElement('a');a.href=shop(`product.html?id=${encodeURIComponent(d.id)}`);a.textContent=`${p.name||'Product'} — ₹${Number(p.price||0)}`;a.style.display='block';return a}));if(!snap.size)root.textContent='No products available.'}catch(e){console.error(e);root.textContent='Unable to load products.'}}load();